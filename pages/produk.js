import Identity from "../components/Identity";
import Layout from "../components/Layout";

export default function Services(params) {
  return (
    <div>
      <Identity>Produk</Identity>
      <Layout></Layout>;
    </div>
  );
}
